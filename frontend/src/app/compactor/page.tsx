import { CompactorModule } from "@/components/compactor/CompactorModule";

export const metadata = { title: "Compactor | Recycle Protocol" };

export default function CompactorPage() {
  return <CompactorModule />;
}
