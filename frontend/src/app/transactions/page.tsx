import { TransactionsModule } from "@/components/transactions/TransactionsModule";

export const metadata = { title: "Transactions | Recycle Protocol" };

export default function TransactionsPage() {
  return <TransactionsModule />;
}
